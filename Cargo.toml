[package]
name = "wasm-nice-pages"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
axum = {version = "0.8.8", default-features = false, features=["json"]}
serde = { version = "1.0.228", features = ["derive"] }
wstd = "0.6.5"
wstd-axum = "0.6.5"
